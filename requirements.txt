# Linux CUDA 12.8 版 PyTorch。其专用的阿里云平铺 wheel 目录由
# script/create_env.sh 的 GRAPH_PYTORCH_WHEEL_INDEX_URL 以 --find-links 注入，
# 避免普通依赖在多个索引间重复解析；请优先通过 `bash script/create_env.sh` 安装。
torch==2.7.1+cu128
transformers==4.57.6
peft==0.19.1
accelerate==1.13.0
huggingface_hub
pandas==2.3.3
numpy==1.26.4
scikit-learn==1.7.1
tqdm==4.67.3
sentencepiece==0.2.1
protobuf==6.32.0
safetensors==0.7.0
